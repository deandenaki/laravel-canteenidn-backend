{
    "private": true,
    "type": "module",
    "scripts": {
        "watch": "php artisan serve",
        "dev": "vite",
        "build": "vite build",
        "migrate": "php artisan migrate",
        "migrate:fresh": "php artisan migrate:fresh"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
